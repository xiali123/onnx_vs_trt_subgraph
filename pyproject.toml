[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "onnx_probe"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "onnx",
    "onnx-graphsurgeon",
    "onnxruntime",
    "numpy",
]