[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "onnx-tools"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "onnx",
    "onnx-graphsurgeon",
    "onnxruntime",
    "numpy",
]

[tool.setuptools.packages.find]
where = ["src"]